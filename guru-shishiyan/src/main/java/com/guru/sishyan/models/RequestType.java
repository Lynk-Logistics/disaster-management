package com.guru.sishyan.models;

/**
 * Type      -> Target
 * Emergency -> victims
 * Demand    -> hub
 * ResourceProvider -> supplier
 */
public enum RequestType {
    EMERGENCY, DEMAND, RESOURCE_PROVIDER
}
